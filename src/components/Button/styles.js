import styled from 'styled-components'

export const NaveButton = styled.button`
    width: ${props => props.width || 'auto'};
    background: #212121;
    color: #fff;
    border:none;
    outline: none;
    padding: .8em;
`
